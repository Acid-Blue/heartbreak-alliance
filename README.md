# 失恋阵线联盟

微信原生小程序 MVP 框架，围绕“阵线小队 + 同伴分享 + 个人 Agent 关怀”搭建。

## 开发方式

1. 使用微信开发者工具导入本目录。
2. AppID 可先使用测试号或在 `project.config.json` 中替换。
3. 云开发环境暂未接入，`app.js` 中 `CLOUD_ENV_ID` 为空时不会初始化云能力。

## Spec

正式规范位于 `docs/spec/`，后续功能开发应先更新 spec，再实现对应页面、服务和验收项。
